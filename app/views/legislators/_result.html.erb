<% results_title(result.district, result.state) -%>
<li class="<%= result.party %>">
	<%= WriteImage( result.bioguide_id ) %>
	<div class="politician_name">
    <span class="politician_name"><%= result.firstname + ' ' + result.middlename + ' ' + result.lastname %></span><br/>
		<%= result.state %><br/>
		<%= WriteLongPartyName(result.party) %><br/>
		<%= district_or_seniority(result.district) %><br/>
	</div>
	<div class="politician_address">
		<b>phone</b> <%= result.phone %> |
		<b>fax</b> <%= result.fax %><br/>
		<%= result.congress_office %><br/>
		<%= WriteTwitterUrl( result.twitter_id, result.title, result.firstname, result.lastname ) %>
		<%= WriteWebsiteUrl( result.website, result.title, result.firstname, result.lastname ) %>
                <%= WriteContactUrl( result.title, result.firstname, result.lastname, result.email, result.webform ) %>		
	</div>
	<br style="clear:both" />
</li>
