	<%= link_to h(@link.title), @link.url %>
	
	<div id="search">
    <% form_tag('/links/results', :method => :get) do %>
      <p>Find Your Representatives</p>
      <%= text_field_tag :address %>
      <%= submit_tag 'Find Your Rep' %>
    <% end %>
	</div>
