<script type="text/javascript" language="javascript">
  function removeToolbar()
  {
     var ELiframe = document.getElementById('TMP__mainframe');
     if( ELiframe != null )
     {
	var url = ELiframe.src;
        document.location.assign(url);
     } else {       
	$('TMP__toolbar').hide('fast');
     }
  }
  function sizeIFrame()
  {
	//need to make this I frame as big as the window.
	var ELiframe = document.getElementById('TMP__mainframe');
	var ELtoolbar = document.getElementById('TMP__toolbar');
	//ELiframe.style.height = document.documentElement.­clientHeight - ELtoolbar.style.height;
  }
  sizeIFrame();
</script>
<style type="text/css">
   div.TMP__toolbar
   {
     background-color:#cccccc;
     margin: 5px solid #8F0F30;
   }
</style>
<div class="TMP__toolbar">
   <a href="javascript:removeToolbar();" ref="nofollow">Remove Toolbar</a>
   <a href="<%= search_url %>" ref="nofollow">Goto Legislators List</a>
   <a href="<%= original_url %>" ref="nofollow">Return</a>
</div>
